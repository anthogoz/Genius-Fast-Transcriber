import { defineWebExtConfig } from 'wxt';

export default defineWebExtConfig({
  disabled: false, // if true, disable the auto-open behavior (if you want to open the extension manually using .output/chrome-mv3-dev)
});
